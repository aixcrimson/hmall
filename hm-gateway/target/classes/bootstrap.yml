spring:
  application:
    name: gateway # 微服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.31.68:8848
      config:
        file-extension: yaml
        shared-configs: # 共享配置
          - dataId: shared-log.yaml # 共享日志配置
